[package]
name = "net-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

# Intentionally dependency-free: keeps the wasm bundle small and avoids the
# getrandom js shim. Randomness is seeded from the host via Rng::new.
[dependencies]
