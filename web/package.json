{
  "name": "bea-web",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Browser version of BeASwitch, BeARouter and the IPv4/IPv6 quiz",
  "scripts": {
    "build:wasm": "cd ../engine && wasm-pack build crates/wasm-api --release --target web --out-dir ../../../web/src/wasm --out-name wasm_api",
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:engine": "node smoke.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.4"
  }
}
